% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inferencia.R
\name{forma_quadratica_qui2}
\alias{forma_quadratica_qui2}
\title{Condicao de distribuicao qui-quadrado de uma forma quadratica}
\usage{
forma_quadratica_qui2(A, Sigma = diag(nrow(A)), tol = 1e-08)
}
\arguments{
\item{A}{Matriz simetrica.}

\item{Sigma}{Matriz de covariancias (padrao identidade).}

\item{tol}{Tolerancia numerica.}
}
\value{
Lista com \code{qui_quadrado} (logico) e \code{gl} = \code{posto(A)}.
}
\description{
\verb{y'Ay / sigma^2} segue qui-quadrado se e somente se \verb{A Sigma} for idempotente.
Os graus de liberdade sao \code{posto(A)}.
}
\details{
Pressupoe \verb{y ~ N(mu, sigma^2 Sigma)}. A funcao checa apenas a condicao
distribucional (\verb{A Sigma} idempotente); a qui-quadrado e nao-central salvo se
\verb{A mu = 0}.
}
\examples{
P <- projetor(llm(y ~ trat, data.frame(
  trat = factor(rep(c("A", "B"), each = 3)), y = c(1, 2, 3, 4, 5, 6))))
forma_quadratica_qui2(P)   # projetor: idempotente => qui-quadrado
}
