% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/anava.R
\name{reducao}
\alias{reducao}
\title{Reducao de soma de quadrados R(.)}
\usage{
reducao(objeto, termos = NULL, ajustado_por = NULL)
}
\arguments{
\item{objeto}{Objeto \code{llm}.}

\item{termos}{Vetor de nomes de termos (p.ex. \code{"trat"} ou \code{c("a", "a:b")}).
\code{NULL} (padrao) usa todos os termos do modelo.}

\item{ajustado_por}{Vetor de termos pelos quais ajustar (entram antes).}
}
\value{
Lista com \code{R} (valor da reducao) e \code{gl} (graus de liberdade).
}
\description{
Calcula \verb{R(mu, termos) = b0' X'y} para o modelo com os termos indicados. Com
\code{ajustado_por}, devolve a reducao parcial \code{R(termos | mu, ajustado_por) = R(mu, ajustado_por, termos) - R(mu, ajustado_por)}, na notacao do curso.
}
\examples{
dados <- data.frame(
  bloco = factor(rep(1:3, times = 4)),
  trat  = factor(rep(c("A", "B", "C", "D"), each = 3)),
  y = c(42, 45, 41, 50, 53, 49, 47, 48, 46, 39, 41, 38)
)
m <- llm(y ~ bloco + trat, dados)
reducao(m, "trat", ajustado_por = "bloco")  # R(tau | mu, b)
}
