% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/llm.R
\name{llm}
\alias{llm}
\title{Ajusta e dichava um modelo linear passo a passo}
\usage{
llm(formula, dados, intercepto = TRUE)
}
\arguments{
\item{formula}{Formula do modelo, p.ex. \code{y ~ bloco + trat}.}

\item{dados}{\code{data.frame} com as variaveis.}

\item{intercepto}{Se \code{TRUE} (padrao), inclui o intercepto \code{mu}.}
}
\value{
Objeto de classe \code{llm}.
}
\description{
Monta a matriz de delineamento completa, o sistema de equacoes normais
(\verb{X'X b = X'y}), calcula posto e deficiencia e registra a deducao em
\code{\link[=passos]{passos()}}. O objeto resultante expoe cada peca (\code{m$X}, \code{m$XtX}, \code{m$Xty}, ...)
e serve de entrada para \code{\link[=betas]{betas()}}, \code{\link[=projetor]{projetor()}} e \code{\link[=eh_estimavel]{eh_estimavel()}}.
}
\examples{
dados <- data.frame(
  trat = factor(rep(c("A", "B", "C"), each = 2)),
  y = c(10, 12, 20, 22, 30, 28)
)
m <- llm(y ~ trat, dados)
m$X
betas(m, restricao = "soma_zero")
}
