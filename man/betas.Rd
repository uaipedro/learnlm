% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/betas.R
\name{betas}
\alias{betas}
\title{Solucoes do sistema de equacoes normais}
\usage{
betas(
  objeto,
  restricao = c("nenhuma", "soma_zero", "casela_referencia"),
  inversa = c("condicional", "moore_penrose", "minimos_quadrados")
)
}
\arguments{
\item{objeto}{Objeto \code{llm}.}

\item{restricao}{Um de \code{"nenhuma"}, \code{"soma_zero"}, \code{"casela_referencia"}.}

\item{inversa}{Quando \code{restricao = "nenhuma"}, qual g-inversa de \verb{X'X} usar:
\code{"condicional"}, \code{"moore_penrose"} ou \code{"minimos_quadrados"}.}
}
\value{
Vetor \code{b} nomeado pelas colunas de X. O atributo \code{ajustados} traz \code{Xb}.
}
\description{
Resolve \verb{X'X b = X'y} de tres modos didaticos: sem restricao (usando uma
g-inversa de \verb{X'X}), ou impondo uma restricao (soma zero ou casela de
referencia). Em todos os casos os valores ajustados \code{Xb} sao identicos — o que
muda e a solucao \code{b}. Essa invariancia e a licao central de modelos de posto
incompleto.
}
\examples{
dados <- data.frame(
  trat = factor(rep(c("A", "B", "C"), each = 2)),
  y = c(10, 12, 20, 22, 30, 28)
)
m <- llm(y ~ trat, dados)
betas(m, restricao = "nenhuma")
betas(m, restricao = "soma_zero")
betas(m, restricao = "casela_referencia")
}
