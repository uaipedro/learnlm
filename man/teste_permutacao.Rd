% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/permutacao.R
\name{teste_permutacao}
\alias{teste_permutacao}
\title{Teste de permutacao (Freedman-Lane) para H0: L beta = m}
\usage{
teste_permutacao(L, objeto, m = NULL, erro = NULL, n_perm = 2000, seed = NULL)
}
\arguments{
\item{L}{Contraste ou conjunto de contrastes: string (p.ex. \code{"trat1 - trat2"}),
vetor de strings, ou matriz \verb{q x p}. Mesma interface de \code{\link[=teste_F]{teste_F()}}.}

\item{objeto}{Objeto \code{llm}.}

\item{m}{Valor(es) sob H0 (padrao zeros). Vetor de comprimento \code{q}.}

\item{erro}{Nome de um termo do modelo a usar como estrato de erro (p.ex.
\code{"bloco:irrig"} em split-plot). \code{NULL} (padrao) permuta livremente e usa o
QME global como denominador.}

\item{n_perm}{Numero de permutacoes Monte Carlo (ignorado se a enumeracao
exata for viavel).}

\item{seed}{Semente para reproducibilidade do Monte Carlo. \code{NULL} nao fixa.}
}
\value{
Objeto de classe \code{permutacao_llm}. Use \code{\link[=print]{print()}}, \code{\link[=plot]{plot()}} e \code{\link[=passos]{passos()}}.
}
\description{
Testa uma ou mais funcoes estimaveis sem pressupor normalidade dos erros.
O esquema de Freedman-Lane permuta os residuos do modelo reduzido (sob H0),
preservando os valores ajustados e — quando \code{erro} e informado — restringindo
a permutacao ao interior de cada estrato.
}
\examples{
dados <- data.frame(
  trat = factor(rep(c("A", "B", "C"), each = 6)),
  y = c(7, 9, 8, 10, 8, 9, 41, 36, 39, 42, 40, 38, 18, 18, 18, 17, 19, 20)
)
m <- llm(y ~ trat, dados)
teste_permutacao("trat1 - trat3", m, seed = 42)
teste_permutacao(c("trat1 - trat2", "trat2 - trat3"), m, seed = 42)
}
