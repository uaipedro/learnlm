% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/boxcox.R
\name{transformar_resposta}
\alias{transformar_resposta}
\title{Transforma a resposta e reajusta o modelo}
\usage{
transformar_resposta(m, lambda = NULL, tipo = NULL)
}
\arguments{
\item{m}{Objeto \code{llm}.}

\item{lambda}{Expoente de Box-Cox; aplica \code{(y^lambda - 1)/lambda} (ou \code{log(y)}
se \code{lambda = 0}).}

\item{tipo}{Transformacao literal: \code{"identidade"}, \code{"log"}, \code{"raiz"},
\code{"inversa_raiz"}, \code{"inversa"} ou \code{"quadrado"}.}
}
\value{
Novo objeto \code{llm}, com \code{m$transformacao} registrando o que foi aplicado.
}
\description{
Aplica uma transformacao normalizadora a \code{y} e reajusta o \code{llm}. Sem \code{lambda}
nem \code{tipo}, usa a transformacao padrao sugerida pelo diagnostico de Box-Cox
guardado em \code{m$boxcox}.
}
\examples{
dados <- data.frame(
  trat = factor(rep(c("A", "B", "C"), each = 4)),
  y = c(1, 2, 1, 3, 12, 18, 15, 20, 80, 95, 70, 110)
)
m <- llm(y ~ trat, dados)
m2 <- transformar_resposta(m, tipo = "log")
m2$transformacao
}
