% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/betas.R
\name{eh_estimavel}
\alias{eh_estimavel}
\title{Testa se uma funcao linear dos parametros e estimavel}
\usage{
eh_estimavel(lambda, objeto, tol = 1e-08)
}
\arguments{
\item{lambda}{Vetor de coeficientes (comprimento = numero de colunas de X).}

\item{objeto}{Objeto \code{llm} ou matriz X.}

\item{tol}{Tolerancia numerica.}
}
\value{
\code{TRUE}/\code{FALSE}. O atributo \code{residuo} traz \verb{max|H lambda - lambda|}.
}
\description{
\verb{lambda' b} e estimavel se e somente se \code{lambda} pertence ao espaco linha de X,
isto e, \verb{H lambda = lambda} com \verb{H = (X'X)^- (X'X)}. Em modelos de posto
incompleto, parametros individuais nao sao estimaveis, mas contrastes sim.
}
\examples{
dados <- data.frame(
  trat = factor(rep(c("A", "B", "C"), each = 2)),
  y = c(10, 12, 20, 22, 30, 28)
)
m <- llm(y ~ trat, dados)
eh_estimavel(c(0, 1, 0, 0), m)   # tau_A sozinho: NAO estimavel
eh_estimavel(c(0, 1, -1, 0), m)  # contraste tau_A - tau_B: estimavel
}
