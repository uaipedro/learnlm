% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/matriz-modelo.R
\name{matriz_modelo}
\alias{matriz_modelo}
\title{Matriz de delineamento completa (super-parametrizada)}
\usage{
matriz_modelo(formula, dados, intercepto = TRUE)
}
\arguments{
\item{formula}{Formula do modelo, p.ex. \code{y ~ bloco + trat + bloco:trat}.}

\item{dados}{\code{data.frame} com as variaveis da formula.}

\item{intercepto}{Se \code{TRUE} (padrao), inclui a coluna \code{mu} (intercepto).}
}
\value{
Matriz X de classe \code{matriz_modelo}, com o atributo \code{descr}
(descritores de cada coluna: termo, niveis, tipo).
}
\description{
Constroi a matriz X com \strong{uma coluna por nivel} de cada fator (mais
interacoes), sem casela de referencia e sem restricoes. Diferente de
\code{model.matrix}, que devolve a matriz ja restrita. Em modelos de posto
incompleto, esta e a X que aparece no material teorico.
}
\examples{
dados <- data.frame(
  trat = factor(rep(c("A", "B", "C"), each = 2)),
  y = c(10, 12, 20, 22, 30, 28)
)
matriz_modelo(y ~ trat, dados)
}
